import { render, screen } from '@testing-library/react';
import App from './App';

describe('App', () => {
  render(<App />);
  it('Sucessfully fetches data for members, and member location.', () => {
    
  })
});
